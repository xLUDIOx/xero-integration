/* cspell:disable */
export enum AccountType {
    FixedAsset = 'FIXED',
    Expense = 'EXPENSE',
}

export enum ClassType {
    Expense = 'EXPENSE',
}
/* cspell:enable */

export enum AccountStatus {
    Active = 'ACTIVE',
    Archived = 'ARCHIVED',
}
